console.log('Script optimisé chargé');

// Chargement différé pour éviter le blocage du rendu
document.addEventListener('DOMContentLoaded', () => {
    console.log('Le DOM est complètement chargé et analysé');
});
